// Shared between the product forms (tagging) and the Proposal Builder
// (matching against style_preferences) — keep these in sync so a tag a
// supplier/admin picks is guaranteed to match something a customer can
// actually select in the builder.
export const STYLE_TAGS = ["Romantic", "Minimalist", "Rustic", "Modern", "Classic"] as const;
